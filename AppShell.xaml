<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="StockAppMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:StockAppMAUI"
    xmlns:views="clr-namespace:StockAppMAUI.Views">

    <FlyoutItem Title="Home">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Products">
        <ShellContent
            ContentTemplate="{DataTemplate views:ProductPage}"/>
    </FlyoutItem>

    <FlyoutItem Title="All transactions">
        <ShellContent
            ContentTemplate="{DataTemplate views:AllTransactionPage}"/>
    </FlyoutItem>
    
</Shell>
